<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <application>
        <!-- Add this inside <application> tag -->
        <meta-data
            android:name="com.ryanheise.foreground_service_notification_channel_name"
            android:value="Screen Capture Service" />
        <meta-data
            android:name="com.ryanheise.foreground_service_notification_channel_description"
            android:value="Service for capturing screen content" />
    </application>
</manifest>
